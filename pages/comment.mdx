# Comment
import { Comment } from '@snippyly/snippyly-demo-component'
import { v4 as uuidv4 } from 'uuid';

export const commentDocumentId = uuidv4();

<Comment user={"Rachel"} documentId={commentDocumentId} height={500}/>