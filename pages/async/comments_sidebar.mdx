import Subheading from '../../components/Subheading/Subheading.tsx';
import StepTabs from '../../components/StepTabs/StepTabs.tsx';
import HTMLSidebarOverview from '../../code_sections/comments_sidebar/HTMLSidebarOverview.tsx';
import ReactSidebarOverview from '../../code_sections/comments_sidebar/ReactSidebarOverview.tsx';
import ReactSidebarCustom from '../../code_sections/comments_sidebar/ReactSidebarCustom.tsx';
import HTMLSidebarCustom from '../../code_sections/comments_sidebar/HTMLSidebarCustom.tsx';
import HTMLSidebarButton from '../../code_sections/comments_sidebar/slots/HTMLSidebarButton.tsx';
import ReactSidebarButton from '../../code_sections/comments_sidebar/slots/ReactSidebarButton.tsx';
import HTMLSidebarEmbed from '../../code_sections/comments_sidebar/HTMLSidebarEmbed.tsx';
import ReactSidebarEmbed from '../../code_sections/comments_sidebar/ReactSidebarEmbed.tsx';

# Comments Sidebar [#comments-sidebar]

<Subheading>Provide a toggleable sidebar to view and filter comments.</Subheading>
{/* <CursorDemo /> */}

# Steps [#steps]

export function SidebarOverviewSteps() {

  const frontendOptions = [
    {
      title: 'React',
      value: <ReactSidebarOverview sectionId="steps" />
    },
    {
      title: 'HTML',
      value: <HTMLSidebarOverview sectionId="steps" />
    }
  ];

  return (
    <StepTabs frontendOptions={frontendOptions} />
  );
}

<SidebarOverviewSteps />

# Customize Behavior [#customize-behavior]

export function SidebarCustomSteps() {
  const frontendOptions = [
    {
      title: 'React',
      value: <ReactSidebarCustom sectionId="customize-behavior" />
    },
    {
      title: 'HTML',
      value: <HTMLSidebarCustom sectionId="customize-behavior" />
    }
  ];

  return (
    <StepTabs frontendOptions={frontendOptions} />
  );
}

<SidebarCustomSteps />

| Field | Type | Description |
| --- | --- | --- |
| documentId | string | The document ID where the comment was added |
| location | string | The location where the comment was added |
| targetElementId | string | The DOM ID of the target element on which the comment was added |
| context | Object \| undefined | Any context data passed when the comment was added |

# Customize UI [#customize-ui]

## 1. Embed Mode

export function SidebarEmbedSteps() {
  const frontendOptions = [
    {
      title: 'React',
      value: <ReactSidebarEmbed sectionId="customize-ui" />
    },
    {
      title: 'HTML',
      value: <HTMLSidebarEmbed sectionId="customize-ui" />
    }
  ];

  return (
    <StepTabs frontendOptions={frontendOptions} />
  );
}

<SidebarEmbedSteps />

## 2. Slots

export function SidebarSlotsSteps() {
  const frontendOptions = [
    {
      title: 'React',
      value: <ReactSidebarButton sectionId="customize-ui" />
    },
    {
      title: 'HTML',
      value: <HTMLSidebarButton sectionId="customize-ui" />
    }
  ];

  return (
    <StepTabs frontendOptions={frontendOptions} />
  );
}

<SidebarSlotsSteps />