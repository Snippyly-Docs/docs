import FlockDemo from '../../components/FlockDemo/FlockDemo.tsx';
import Subheading from '../../components/Subheading/Subheading.tsx';
import HTMLFlockOverview from '../../code_sections/flock/HTMLFlockOverview.tsx';
import ReactFlockOverview from '../../code_sections/flock/ReactFlockOverview.tsx';
import StepTabs from '../../components/StepTabs/StepTabs.tsx';
import ReactFlockCustom from '../../code_sections/flock/ReactFlockCustom.tsx';
import HTMLFlockCustom from '../../code_sections/flock/HTMLFlockCustom.tsx';
import ReactFlockAPI from '../../code_sections/flock/ReactFlockAPI.tsx';
import HTMLFlockAPI from '../../code_sections/flock/HTMLFlockAPI.tsx';

# Flock Mode [#flock-mode]

<Subheading>Start a flock session and follow the leader with a single click.</Subheading>
<FlockDemo />

# Steps [#steps]

export function FlockSteps() {

  const frontendOptions = [
    {
      title: 'React',
      value: <ReactFlockOverview sectionId="steps" />
    },
    {
      title: 'HTML',
      value: <HTMLFlockOverview sectionId="steps" />
    }
  ];

  return (
    <StepTabs frontendOptions={frontendOptions} />
  );
}

<FlockSteps />

# Customize Behavior [#customize-behavior]

export function FlockCustomSteps() {

  const frontendOptions = [
    {
      title: 'React',
      value: <ReactFlockCustom sectionId="customize-behavior" />
    },
    {
      title: 'HTML',
      value: <HTMLFlockCustom sectionId="customize-behavior" />
    }
  ];

  return (
    <StepTabs frontendOptions={frontendOptions} />
  );
}

<FlockCustomSteps />

## PageInfo

| Field | Type | Description |
| --- | --- | --- |
| baseUrl | string | The base URL of the site |
| path | string | The path after the base URL |
| url | string | The full URL |

# API [#api]

export function FlockAPISteps() {

    const frontendOptions = [
      {
        title: 'React',
        value: <ReactFlockAPI sectionId="api" />
      },
      {
        title: 'HTML',
        value: <HTMLFlockAPI sectionId="api" />
      }
    ];
  
    return (
      <StepTabs frontendOptions={frontendOptions} />
    );
}

<FlockAPISteps />