# Huddle

import { Huddle } from '@snippyly/snippyly-demo-component'
import { v4 as uuidv4 } from 'uuid';

export const huddleDocumentId = uuidv4();

<Huddle user={"Rachel"} documentId={huddleDocumentId}/>