import Subheading from '../../components/Subheading/Subheading.tsx';
import StepTabs from '../../components/StepTabs/StepTabs.tsx';
import HTMLInstallation from '../../code_sections/installation/HTMLInstallation.tsx';
import ReactInstallation from '../../code_sections/installation/ReactInstallation.tsx';
import HTMLConfig from '../../code_sections/installation/HTMLConfig.tsx';
import ReactConfig from '../../code_sections/installation/ReactConfig.tsx';
import HTMLUsers from '../../code_sections/installation/HTMLUsers.tsx';
import ReactUsers from '../../code_sections/installation/ReactUsers.tsx';
import ReactContactList from '../../code_sections/installation/ReactContactList.tsx';
import ReactLocation from '../../code_sections/installation/ReactLocation.tsx';
import HTMLLocation from '../../code_sections/installation/HTMLLocation.tsx';

import { NextSeo } from 'next-seo';

# Install [#install]

<NextSeo 
  title="Velt Docs - Installation"
  description="Installation documentation page for the Velt SDK"
/>

export function InstallSteps() {

  const frontendOptions = {
    'React': <ReactInstallation sectionId="install" />,
    'Next.js': <ReactInstallation sectionId="install" />,
    'HTML': <HTMLInstallation sectionId="install" />
  };

  return (
    <StepTabs 
      frontendOptions={frontendOptions}
    />
  );
}

<InstallSteps />

<br />
## Advanced Configurations [#configure]
**The <code>SnippylyProvider</code> component takes a <code>config</code> property.**

You can pass in a configuration object that affects the SDK at a global level across your application.
<br />
export function ConfigSteps() {
  const frontendOptions = {
    'React': <ReactConfig sectionId="configure" />,
    'Next.js': <ReactConfig sectionId="configure" />,
    'HTML': <HTMLConfig sectionId="configure" />
  };

  return (
    <StepTabs 
      frontendOptions={frontendOptions}
    />
  );
}

<ConfigSteps />

# Authenticate [#authenticate]
**This tells us who the logged in user is so that we can associate the data with them.**

As soon as you get your authenticated user, you should call the SDK's `identify()` method.

<br />

export function UserSteps() {
  const frontendOptions = {
    'React': <ReactUsers sectionId="authenticate" />,
    'Next.js': <ReactUsers sectionId="authenticate" />,
    'HTML': <HTMLUsers sectionId="authenticate" />
  };

  return (
    <StepTabs frontendOptions={frontendOptions} />
  );
}

<UserSteps />

## Enable @ mentions, user tagging [#user-tagging]
export function UserContactList() {
  const frontendOptions = {
    'React': <ReactContactList sectionId="user-contact-list" />,
    'Next.js': <ReactContactList sectionId="user-contact-list" />,
    'HTML': <ReactContactList sectionId="user-contact-list" />
  };

  return (
    <StepTabs frontendOptions={frontendOptions} />
  );
}

<UserContactList />


## Additional options [#additional-options]

# Initialize Document [#initDocument]

export function LocationSteps() {
  const frontendOptions = {
    'React': <ReactLocation sectionId="initDocument" />,
    'Next.js': <ReactLocation sectionId="initDocument" />,
    'HTML': <HTMLLocation sectionId="initDocument" />
  };

  return (
    <StepTabs frontendOptions={frontendOptions} />
  );
}

<LocationSteps />
